# Node.js / TypeScript Binding

This binding will expose the Dopax runtime to Node.js and TypeScript applications.

Planned direction:

- thin wrapper around `dopax-ffi`
- JSON-oriented request and response boundary
- ergonomic async iterator for streaming responses
- no provider logic duplicated in JavaScript or TypeScript
