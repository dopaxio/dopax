# Java Binding

This directory contains the JVM-facing binding scaffold for Dopax.

Planned direction:

- thin wrapper around `dopax-ffi`
- Java-first ergonomic API without duplicating provider logic
- streaming support on top of shared ABI handles
